const HOST = 'localhost'
const PORT = '3000'
const URL = 'http://' + HOST + ':' + PORT
export const ROUTES = {
    login: '/api/login',
    signup: '/api/signup',
    auth: '/api/auth',
    taxableData: '/api/taxableData',
}
